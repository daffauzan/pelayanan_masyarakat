services:
  app:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    image: pelayanan-publik-app
    container_name: pelayanan-publik
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - ../.env
